import { DTBundle } from '~types/dataTypes';
import { initialState, Example, Options, Help } from './OrganizationNumber.ui';
import { getMetadata } from './OrganizationNumber.generate';

const bundle: DTBundle = {
	initialState,
	Example,
	Options,
	Help,
	getMetadata
};

export default bundle;
