import React from 'react';
// import clsx from 'clsx';
import styles from './HomepageFeatures.module.css';

export default function HomepageFeatures () {
	return (
		<section className={styles.features}>
			<div className="container">
				<div className="row">
					Welcome! This is the generatedata.com v4 documentation. As you can see, it's a work in progress,
					but it's currently top of the list of items to focus on. So stop back in a few days to see the progress.
				</div>
			</div>
		</section>
	);
}
