import { DTDefinition } from '../../../../types/dataTypes';

const definition: DTDefinition = {
	name: 'GUID',
	fieldGroup: 'numeric',
	fieldGroupOrder: 50,
	exports: [
		'Help', 'getMetadata'
	]
};

export default definition;
