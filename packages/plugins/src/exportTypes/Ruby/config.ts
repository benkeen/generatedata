import type { ETDefinition } from '~types/exportTypes';

const definition: ETDefinition = {
  fieldGroup: 'programmingLanguage',
  codeMirrorModes: ['clike/clike', 'ruby/ruby']
};

export default definition;
