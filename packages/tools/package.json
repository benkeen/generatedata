{
  "name": "@generatedata/tools",
  "description": "General tooling and binaries for generatedata packages",
  "version": "1.0.0",
  "bin": {
    "sort-i18n-files": "./bin/sort-i18n-files.js",
    "validate-i18n-files": "./bin/validation-i18n-files.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "npx rimraf dist",
    "purge": "npx rimraf node_modules dist .turbo",
    "test": "jest --config=./config/jest.config.js"
  },
  "devDependencies": {
    "@jest/globals": "catalog:",
    "@types/jest": "catalog:",
    "jest": "catalog:",
    "rimraf": "catalog:",
    "ts-jest": "catalog:",
    "typescript": "catalog:"
  }
}
