import { makeStyles } from '@griffel/react';

export const useClasses = makeStyles({
  options: {
    '& label': {
      marginRight: '2px'
    },
    '& input': {
      marginRight: '2px'
    }
  }
});
