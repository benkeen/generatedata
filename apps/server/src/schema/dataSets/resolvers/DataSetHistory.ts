import type { DataSetHistoryResolvers } from './../../types.generated';

export const DataSetHistory: DataSetHistoryResolvers = {
  // no custom logic here needed. File left as-is so it's not regenerated each time
};
