{
	"title": "HTML",
	"$schema": "http://json-schema.org/draft-04/schema#",
	"type": "object",
	"properties": {
		"stripWhitespace": { // TODO 1/0
			"type": "boolean"
		},
		"dataStructureFormat": {
			"enum": ["simple", "complex"]
		}
	},
	"required": ["stripWhitespace", "dataStructureFormat"]
}

