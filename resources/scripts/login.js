/*global $:false,browser:true*/
require([
	"manager",
	"pluginManager",
	"lang",
	"utils",
	"pageinit"
], function(manager, pluginManager, L, utils) {

	"use strict";

	$(function() {
		manager.start();

	});
});
