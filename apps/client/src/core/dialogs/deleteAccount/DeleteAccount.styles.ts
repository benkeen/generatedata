import { makeStyles } from '@griffel/react';

export const useClasses = makeStyles({
  contentPanel: {
    padding: '10px 16px'
  },
  accountName: {
    fontSize: '18px',
    marginTop: '5px',
    color: '#888888'
  }
});
