import { DTBundle } from '../../../../types/dataTypes';
import { Options, Help } from './TextFixed.ui';
import { generate, getMetadata } from './TextFixed.generate';

const bundle: DTBundle = {
	Options,
	Help,
	generate,
	getMetadata
};

export default bundle;