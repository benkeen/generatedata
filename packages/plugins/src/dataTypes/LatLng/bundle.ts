import { DTBundle } from '../../';
import { Help, Options, getMetadata } from './LatLng';
import { initialState } from './LatLng.state';

const bundle: DTBundle = {
  initialState,
  Options,
  Help,
  getMetadata
};

export default bundle;
