import { Plugin } from 'rollup';
import { StripExportsOptions } from './options';
declare const _default: (options?: StripExportsOptions) => Plugin;
/**
 * Rollup plugin which strips export statements.
 *
 * @param options - Strip exports options.
 * @returns Strip export plugin.
 */
export = _default;
